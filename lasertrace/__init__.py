"""LaserTrace Pro core: laser-first black-and-white vectorizer.

The core package is UI-agnostic (no Qt imports) and every pipeline stage is a
pure function so it can be unit-tested and regressed from the CLI.
"""

__version__ = "0.1.0"
