# Core + test dependencies for a checkout, editable.
# pyproject.toml is the single source of truth for versions; this file exists
# so `pip install -r requirements.txt` still works out of a clone.
-e .[dev]
